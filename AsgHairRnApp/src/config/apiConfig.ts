/**
 * Central API configuration for the React Native app.
 * Change BASE_URL here to match your production domain or local testing IP.
 * - On Android Emulators, 'http://10.0.2.2:3000' routes to the localhost of the host machine.
 * - On physical devices, use the local IP of your development machine (e.g. 'http://192.168.1.5:3000').
 */
export const BASE_URL = 'http://10.0.2.2:3000';
